{
  "name": "chat-restyler-for-youtube",
  "private": true,
  "version": "1.2.0",
  "type": "module",
  "description": "Chrome extension that makes the YouTube Live chat look and read better.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "package": "npm run build && node scripts/package.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.34",
    "@types/adm-zip": "^0.5.8",
    "@types/chrome": "^0.0.287",
    "adm-zip": "^0.5.17",
    "typescript": "^5.7.2",
    "vite": "^6.0.0"
  }
}
